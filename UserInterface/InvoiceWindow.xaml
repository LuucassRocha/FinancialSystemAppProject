<Window x:Class="UserInterface.InvoiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UserInterface"
        mc:Ignorable="d"
        Title="Invoice Registration" Height="550" Width="600" WindowStartupLocation="CenterScreen">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Text="Invoice Registration"
                   FontSize="20" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>
        
        <!-- Form -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">

            <Label Content="Invoice Number:"/>
            <TextBox x:Name="txtInvoiceNumber" Margin="0,0,0,10"/>

            <Label Content="Issue Date:"/>
            <DatePicker x:Name="dpIssueDate" Margin="0,0,0,10"/>

            <Label Content="Value (R$):"/>
            <TextBox x:Name="txtValue" Margin="0,0,0,10"/>

            <Label Content="Description:"/>
            <TextBox x:Name="txtDescription" Margin="0,0,0,10" Height="60" TextWrapping="Wrap"/>

            <Label Content="Creditor:"/>
            <ComboBox x:Name="cmbCreditors"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      Margin="0,0,0,10"/>

            <Button x:Name="btnAddInvoice"
                    Content="Add Invoice"
                    Click="BtnAddInvoice_Click"
                    Background="LightBlue"
                    Padding="10"
                    Margin="0,10,0,0"/>
        </StackPanel>
            
        <!-- Invoices List -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <Label Content="Registered Invoices:" FontWeight="Bold"/>
            <ListBox x:Name="lstInvoices" Height="150"/>
            <TextBlock x:Name="txtCounter" Margin="0,5,0,0" FontStyle="Italic"/>
        </StackPanel>
    </Grid>
</Window>
